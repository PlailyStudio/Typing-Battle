# 실시간 타자배틀 웹게임 기획서

## 1. 개요

Nakama를 활용해 여러 플레이어가 같은 문장을 입력하며 경쟁하는 웹게임이다. 혼자 연습하는 싱글플레이와 방을 만들어 함께 즐기는 멀티플레이를 제공한다.

## 2. 메인 페이지

메인 페이지는 **싱글플레이 / 방 생성 / 방 참가** 세 개의 탭으로 구성한다. 각 탭에서 필요한 설정을 선택한 뒤 게임을 시작하거나 대기실에 입장한다.

| 탭 | 주요 기능 |
| --- | --- |
| 싱글플레이 | 닉네임 입력, 연습 시작, 개인 기록 확인 |
| 방 생성 | 닉네임·방 이름·최대 인원 설정, 방 생성 후 참가 코드 표시 |
| 방 참가 | 닉네임과 참가 코드 입력, 해당 방 대기실 입장 |

초기 버전은 별도 회원가입 없이 닉네임을 정해 이용하는 흐름을 기본으로 한다.

## 3. 플레이 흐름

### 싱글플레이

싱글플레이 탭 → 연습 시작 → 카운트다운 → 문장 입력 → 결과 확인 → 다시 시작 또는 메인으로 이동

### 멀티플레이

방 생성 또는 참가 → 대기실 → 참가자 준비 → 방장 시작 → 카운트다운 → 동시 경기 → 결과 확인 → 대기실 복귀

- 대기실에는 참가자 목록, 방장 표시, 준비 상태, 참가 코드를 보여준다.
- 방장을 제외한 모든 참가자가 준비하면 방장이 경기를 시작할 수 있다.
- 경기 중인 방에는 새로 참가할 수 없도록 한다.

## 4. 인게임 화면

**내 플레이 화면은 아래쪽에 크게 배치하고, 다른 플레이어의 플레이 화면은 위쪽에 작게 나란히 표시한다.** TETR.IO처럼 상대가 실제로 플레이하는 모습을 함께 볼 수 있는 구성을 지향한다.

```text
┌──────────────────────────────────────────┐
│ 상대 플레이 화면                         │
│ ┌────────────────┐ ┌────────────────┐   │
│ │ 플레이어 A     │ │ 플레이어 B     │   │
│ │ 입력할 문장    │ │ 입력할 문장    │   │
│ │ 현재 입력·커서 │ │ 현재 입력·커서 │   │
│ │ 오타·입력 상태 │ │ 오타·입력 상태 │   │
│ └────────────────┘ └────────────────┘   │
├──────────────────────────────────────────┤
│ 남은 시간 / 내 순위 / 경기 상태          │
├──────────────────────────────────────────┤
│ 내 플레이 영역                           │
│ 입력해야 할 문장                         │
│ 입력 완료·현재 위치·오타 구분 표시       │
│ [현재 입력 중인 내용                  ] │
│ 내 진행률 / 타수 / 정확도                │
└──────────────────────────────────────────┘
```

- 상대 영역은 내 플레이 화면과 같은 구성을 축소한 미니 플레이 화면으로 만든다. 각 화면에는 닉네임, 현재 풀고 있는 문장, 입력 중인 내용, 커서 위치, 정답·오타 표시를 보여준다.
- 상대가 글자를 입력하거나 지우고, 오타를 수정하거나 다음 문장으로 넘어가는 모습이 실시간으로 반영된다. 한글 조합 중인 글자도 입력 중 상태로 표시한다.
- 상대 화면은 읽기 전용이며, 타수와 순위는 보조 정보로 작게 표시한다. 4인 경기에서는 상대 화면 3개를 상단에 배치한다.
- 내 영역은 입력에 집중할 수 있도록 충분한 크기로 고정 배치한다.
- 올바르게 입력한 글자, 현재 입력 위치, 오타를 색상과 밑줄로 구분한다.
- 싱글플레이에서는 상대 영역에 개인 최고 기록과 현재 연습 정보를 표시한다.
- 초기 버전은 PC 키보드와 한글 입력을 우선 지원하며, 한글 조합 중인 글자를 확정된 오타로 처리하지 않는다.

## 5. 기본 경기 규칙 — MVP 제안

- 멀티플레이 인원은 2~4명으로 한다.
- 모든 참가자에게 같은 문장 목록을 같은 순서로 제공한다.
- 경기 시작 전 3초 카운트다운을 표시하고, 제한 시간은 60초로 한다.
- 현재 문장을 정확하게 입력하면 다음 문장으로 넘어간다. 오타는 수정해야 한다.
- 진행률은 전체 목표 글자 중 순서대로 정확히 입력한 글자의 비율로 계산한다.
- 제한 시간 내 전체 문장을 완료한 참가자는 완료 시간이 빠른 순서로 순위를 정한다.
- 미완료 참가자는 진행률, 정확도 순으로 순위를 정하고, 둘 다 같으면 공동 순위로 처리한다.
- 제한 시간이 끝나거나 모든 참가자가 완료하면 경기를 종료한다.
- 결과 화면에는 최종 순위, 진행률, 타수, 정확도, 완료 여부를 표시한다.
- 타수는 분당 확정 입력 글자 수로 계산하고, 정확도는 전체 확정 입력 시도 중 정답 비율로 계산한다. 한글 조합 중간 이벤트는 집계에서 제외한다.

## 6. Nakama 활용 계획

아래는 구현 시 적용할 역할 분담이다.

| 영역 | 역할 |
| --- | --- |
| 사용자 세션 | 게스트 인증과 플레이어 식별 |
| 방 관리 | 참가 코드에 따른 방 조회·입장, 인원 제한과 준비 상태 관리 |
| 실시간 통신 | 대기실 변경 사항, 경기 시작, 참가자의 문장 위치·현재 입력·커서·한글 조합 상태, 경기 결과 전달 |
| 서버 경기 로직 | 공통 문장과 시작·종료 시각 관리, 입력 검증, 최종 순위 확정 |
| 기록 저장 | 개인 최고 기록과 경기 결과 저장 |

클라이언트는 입력과 화면 표시를 담당하고, Nakama 서버는 경기 상태와 결과의 기준을 관리한다. 로컬 입력 피드백은 즉시 표시하고, 상대 플레이 화면은 서버를 통해 전달받은 게임 내 입력 상태로 재현한다. 이를 위해 내 화면과 상대 화면에 같은 타자 화면 컴포넌트를 사용한다.

상대 화면은 문장 번호, 입력 문자열, 커서 위치, 한글 조합 상태를 동기화해 그린다. 화면 영상 전송은 필요하지 않다. 표시용 조합 상태와 점수 계산용 확정 입력을 구분하고, 클라이언트가 보낸 점수와 완료 여부를 그대로 신뢰하지 않도록 한다.

## 7. 예외 처리

- 존재하지 않는 참가 코드, 인원 초과, 경기 중인 방은 입장 불가 사유를 안내한다.
- 대기실에서 방장이 나가면 남은 참가자 중 한 명에게 방장을 넘긴다.
- 경기 중 연결이 끊긴 참가자는 이탈로 표시하고 다른 참가자의 경기는 계속한다.
- 초기 버전에서는 경기 중 재접속 복귀를 지원하지 않는다.
- 참가자가 모두 나간 방은 정리한다.

## 8. 개발 범위

**1차 구현:** 메인 3개 탭, 싱글플레이, 방 생성·코드 참가, 대기실 준비, 상대 미니 플레이 화면과 실시간 입력 동기화, 실시간 경기, 결과 화면, 개인 최고 기록 저장.

**추후 확장:** 공개 방 목록, 난이도·언어 선택, 자동 매칭, 랭킹, 친구 초대, 경기 중 재접속.
