import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages project sites are served from /<repository>/.
  // Relative asset URLs work both there and during local development.
  base: './',
});
